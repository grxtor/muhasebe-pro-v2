# pnpm: hoisted (flat) node_modules — Docker/Next.js standalone uyumluluğu için
# Prisma client'ın node_modules/.prisma'da olması gerekiyor (symlink değil gerçek dosya)
node-linker=hoisted
shamefully-hoist=true
